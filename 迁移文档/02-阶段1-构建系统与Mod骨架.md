# RTSbuilding 阶段 1 — 构建系统与 Mod 骨架

## 状态：✅ 完成

## 日期：2026-06-07

## 目标
搭建 Forge 1.7.10 构建骨架 + Mod 入口类 + 配置 + 实体

## 执行记录

### 构建系统
- `build.gradle.kts` — GTNH Convention 插件
- `settings.gradle.kts` — GTNH Settings Convention 2.0.20
- `gradle.properties` — modId=rtsbuilding, enableModernJavaSyntax=jvmDowngrader, jvmDowngraderStubsProvider=gtnhlib, usesMixins=true
- `dependencies.gradle` — api: GTNHLib 0.7.10
- `repositories.gradle`, `jitpack.yml`, `.gitignore`, `.gitattributes`, `.editorconfig`, `.java-version`
- `gradle/wrapper/` — Gradle 9.3.1
- `gradlew` / `gradlew.bat`
- `gtnhShared/spotless.gradle` / `spotless.importorder` / `spotless.eclipseformat.xml`

### Java 源文件
| 文件 | 说明 |
|------|------|
| `RtsbuildingMod.java` | @Mod + @Instance + @SidedProxy + @EventHandler |
| `CommonProxy.java` | EntityRegistry 注册 RtsCameraEntity, FML 事件总线 (Login/Logout/ServerStarted) |
| `ClientProxy.java` | 继承 CommonProxy |
| `Config.java` | Forge Configuration + 7 配置项 + 热更新 set()/save() |
| `entity/RtsCameraEntity.java` | Forge 1.7.10 Entity (World 构造, entityInit/NBT, snapTo) |
| `RtsCommunityLinks.java` | 原样复用 |
| `common/BuilderMode.java` | 原样复用 |
| `mixin/ChestMenuMixin.java` | ContainerChest.canInteractWith (阶段 2 后提供 RemoteMenuCompat 桩) |
| `compat/remote/RtsRemoteMenuCompat.java` | 桩类 (编译依赖) |

### 资源
- `mcmod.info` — ${modId}/${modName}/${modVersion} 占位符
- `pack.mcmeta` — pack_format=1
- `rtsbuilding.mixins.json` — ChestMenuMixin, JAVA_8

### 编译修复
- 阶段 1 初版编译报错 PlayerEvent 歧义 → 使用完全限定名解决
- PlayerChangedDimensionEvent 在 1.7.10 不存在 → 移除桩监听器
- RtsRemoteMenuCompat 桩解除 ChestMenuMixin 编译依赖

## 验证
`gradlew build` SUCCESSFUL